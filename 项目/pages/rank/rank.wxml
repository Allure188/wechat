<view class="img">
  <image src="/pages/image/tuijian.jpg"></image>
</view>
<view class="tuijian" wx:for="{{data}}" wx:key="key" wx:if="{{item.tui==1}}">
  <view>
    <image src="{{item.pic}}"></image>
  </view>
  <view class="write">
    <view class="samll">{{item.sing}}</view>
    <view class="samll">{{item.geshou}}</view>
  </view>
</view>